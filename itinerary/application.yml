spring:
  application:
    name: itinerary # Debe llamarse itinerary, no login
  datasource:
    # Debes apuntar a un nuevo puerto, el checklist sugiere 3313 para itinerario
    url: jdbc:mysql://localhost:3313/itinerary 
    username: examen
    password: examen123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 9003 # Puerto único para este servicio

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/